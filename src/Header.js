import React from "react";

import "./Header.css";

const Header = () => {
  return (
    <div className="header">
      <button>Favourites</button>
      <button>Feed</button>
    </div>
  );
};

export default Header;
